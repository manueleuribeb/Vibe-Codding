{
  "name": "energia-forecast",
  "private": true,
  "scripts": {
    "start-backend": "python start-backend.py",
    "dev": "npm --prefix frontend run dev",
    "dev-frontend": "npm --prefix frontend run dev"
  }
}
